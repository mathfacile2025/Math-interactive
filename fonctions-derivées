<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visual Derivatives - Interactive Demo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(135deg, #0a0a1a, #1a1a3a);
            color: white;
            overflow-x: hidden;
            min-height: 100vh;
        }

        .github-header {
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(10px);
            padding: 20px;
            text-align: center;
            border-bottom: 2px solid rgba(0, 245, 255, 0.3);
        }

        .github-title {
            font-size: 3em;
            font-weight: bold;
            background: linear-gradient(45deg, #00f5ff, #ff00f5);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
        }

        .github-subtitle {
            font-size: 1.2em;
            color: #888;
            margin-bottom: 15px;
        }

        .github-badges {
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
        }

        .badge {
            background: linear-gradient(45deg, #00f5ff, #0088cc);
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(0, 245, 255, 0.3);
        }

        .main-container {
            display: flex;
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px;
            gap: 30px;
        }

        .demo-section {
            flex: 2;
            background: rgba(0, 0, 0, 0.3);
            border: 3px solid rgba(0, 245, 255, 0.5);
            border-radius: 20px;
            padding: 20px;
            backdrop-filter: blur(10px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
        }

        .code-section {
            flex: 1;
            background: rgba(0, 0, 0, 0.8);
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: 15px;
            padding: 20px;
            backdrop-filter: blur(10px);
            max-height: 600px;
            overflow-y: auto;
        }

        #mainCanvas {
            border-radius: 15px;
            background: radial-gradient(circle at center, rgba(0, 20, 40, 0.9), rgba(0, 0, 0, 0.9));
            box-shadow: inset 0 0 50px rgba(0, 245, 255, 0.1);
            display: block;
            margin: 0 auto;
        }

        .controls-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 10px;
            margin: 20px 0;
        }

        .control-btn {
            padding: 12px;
            background: linear-gradient(45deg, #00f5ff, #0088cc);
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 245, 255, 0.3);
        }

        .control-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 245, 255, 0.5);
        }

        .values-display {
            background: rgba(0, 245, 255, 0.1);
            border: 2px solid #00f5ff;
            border-radius: 10px;
            padding: 15px;
            margin: 15px 0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
        }

        .value-item {
            text-align: center;
            padding: 10px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 8px;
        }

        .value-label {
            font-size: 0.9em;
            color: #aaa;
            margin-bottom: 5px;
        }

        .value-number {
            font-family: 'Courier New', monospace;
            font-size: 1.3em;
            color: #00f5ff;
            font-weight: bold;
            text-shadow: 0 0 5px #00f5ff;
        }

        .code-block {
            background: #1a1a1a;
            border: 1px solid #333;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
            overflow-x: auto;
        }

        .code-title {
            color: #00f5ff;
            font-size: 1.1em;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .formula {
            background: rgba(255, 0, 245, 0.1);
            border: 2px solid #ff00f5;
            border-radius: 10px;
            padding: 15px;
            margin: 15px 0;
            text-align: center;
            font-size: 1.2em;
            color: #ff00f5;
            font-weight: bold;
        }

        .interaction-hint {
            background: rgba(255, 255, 0, 0.1);
            border: 2px solid #ffff00;
            border-radius: 10px;
            padding: 15px;
            margin: 15px 0;
            text-align: center;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.8; }
            50% { opacity: 1; }
        }

        @media (max-width: 1200px) {
            .main-container {
                flex-direction: column;
            }
            
            .code-section {
                max-height: none;
            }
        }
    </style>
</head>
<body>
    <div class="github-header">
        <h1 class="github-title">Dérivées Visuelles</h1>
        <p class="github-subtitle">Visualisation Mathématique Interactive</p>
        <div class="github-badges">
            <span class="badge">📊 Éducatif</span>
            <span class="badge">🎮 Interactif</span>
            <span class="badge">📱 Responsive</span>
            <span class="badge">🔊 Audio</span>
        </div>
    </div>

    <div class="main-container">
        <div class="demo-section">
            <canvas id="mainCanvas" width="500" height="500"></canvas>
            
            <div class="controls-grid">
                <button class="control-btn" onclick="nextStep()" id="nextBtn">▶️ Commencer</button>
                <button class="control-btn" onclick="toggleAutoMode()" id="autoBtn">🤖 Auto</button>
                <button class="control-btn" onclick="resetDemo()" id="resetBtn">🔄 Reset</button>
                <button class="control-btn" onclick="toggleSound()" id="soundBtn">🔊 Son</button>
            </div>

            <div class="values-display">
                <div class="value-item">
                    <div class="value-label">Position X</div>
                    <div class="value-number" id="currentX">0.0</div>
                </div>
                <div class="value-item">
                    <div class="value-label">f(x) Value</div>
                    <div class="value-number" id="currentY">0.0</div>
                </div>
                <div class="value-item">
                    <div class="value-label">Tangent Slope</div>
                    <div class="value-number" id="currentSlope">0.0</div>
                </div>
                <div class="value-item">
                    <div class="value-label">f'(x) Derivative</div>
                    <div class="value-number" id="currentDerivative">0.0</div>
                </div>
            </div>

            <div class="interaction-hint" id="hintBox">
                🖱️ Cliquez sur "Commencer" pour explorer les dérivées visuellement !
            </div>
        </div>

        <div class="code-section">
            <div class="code-title">📝 Implémentation Mathématique</div>
            
            <div class="formula">
                f(x) = 0.05x² - 100<br>
                f'(x) = 0.1x
            </div>

            <div class="code-block">
                <div class="code-title">Définition des Fonctions</div>
                <pre><code>function f(x) {
    return 0.05 * x * x - 100;
}

function fPrime(x) {
    return 0.1 * x; // Dérivée
}</code></pre>
            </div>

            <div class="code-block">
                <div class="code-title">Calcul de la Tangente</div>
                <pre><code>function drawTangent() {
    const pente = fPrime(currentX);
    const y = f(currentX);
    
    // y - y₀ = m(x - x₀)
    const dx = 50;
    const dy = pente * dx;
    
    drawLine(x₀ - dx, y₀ + dy,
             x₀ + dx, y₀ - dy);
}</code></pre>
            </div>

            <div class="code-block">
                <div class="code-title">Concepts Clés</div>
                <pre><code>• Dérivée = taux de variation instantané
• f'(x) = pente de la tangente
• f'(x) = 0 → points critiques
• f'(x) > 0 → fonction croissante
• f'(x) < 0 → fonction décroissante</code></pre>
            </div>

            <div class="code-block">
                <div class="code-title">🎯 Progression des Étapes</div>
                <pre><code>1. Affichage fonction de base
2. Introduction point mobile  
3. Tracé de la tangente
4. Calcul de la pente
5. Construction graphique dérivée
6. Analyse points critiques
7. Exploration interactive</code></pre>
            </div>
        </div>
    </div>

    <script>
        // Variables globales
        let currentStep = 0;
        let isAutoPlaying = false;
        let autoMode = false;
        let autoInterval;
        let currentX = 0;
        let isDragging = false;
        let showTangent = false;
        let showDerivative = false;
        let soundEnabled = true;

        // Audio context
        const audioContext = new (window.AudioContext || window.webkitAudioContext)();

        // Canvas setup
        const canvas = document.getElementById('mainCanvas');
        const ctx = canvas.getContext('2d');
        const centerX = 250;
        const centerY = 250;

        // Fonctions mathématiques
        function f(x) {
            return 0.05 * x * x - 100;
        }

        function fPrime(x) {
            return 0.1 * x;
        }

        function toCanvasX(x) {
            return centerX + x * 2;
        }

        function toCanvasY(y) {
            return centerY - y * 2;
        }

        function fromCanvasX(canvasX) {
            return (canvasX - centerX) / 2;
        }

        // Audio functions
        function speak(text, rate = 1) {
            if ('speechSynthesis' in window && soundEnabled) {
                speechSynthesis.cancel();
                const utterance = new SpeechSynthesisUtterance(text);
                utterance.lang = 'fr-FR';
                utterance.rate = rate;
                utterance.pitch = 1;
                utterance.volume = 0.8;
                speechSynthesis.speak(utterance);
            }
        }

        function playTone(frequency, duration = 200) {
            if (!soundEnabled) return;
            const oscillator = audioContext.createOscillator();
            const gainNode = audioContext.createGain();
            
            oscillator.connect(gainNode);
            gainNode.connect(audioContext.destination);
            
            oscillator.frequency.setValueAtTime(frequency, audioContext.currentTime);
            oscillator.type = 'sine';
            
            gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + duration / 1000);
            
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + duration / 1000);
        }

        // Étapes
        const steps = [
            {
                title: "Visualisation de Fonction",
                explanation: "Fonction mathématique f(x) = 0.05x² - 100 affichée avec système de coordonnées.",
                audio: "Bienvenue dans les dérivées visuelles ! Voici notre fonction f de x égale 0.05 x au carré moins 100.",
                action: () => {
                    drawAxes();
                    drawFunction();
                    updateHint("📊 Fonction parabole de base affichée");
                }
            },
            {
                title: "Point Mobile",
                explanation: "Le point rouge se déplace le long de la courbe, montrant la position actuelle pour le calcul de dérivée.",
                audio: "Ce point rouge se déplace le long de la courbe, montrant où nous calculons la dérivée.",
                action: () => {
                    currentX = -60;
                    drawEverything();
                    updateHint("🔴 Point positionné pour l'analyse");
                }
            },
            {
                title: "Ligne Tangente",
                explanation: "La ligne tangente jaune touche la courbe en exactement un point sans la traverser.",
                audio: "La ligne tangente jaune touche la courbe en exactement un point.",
                action: () => {
                    showTangent = true;
                    drawEverything();
                    updateHint("📏 Ligne tangente calculée et affichée");
                }
            },
            {
                title: "Calcul de Pente",
                explanation: "Pente = montée/distance. Le triangle jaune montre comment calculer la pente de la tangente.",
                audio: "La pente égale montée sur distance. Voyez comment on la calcule avec le triangle jaune.",
                action: () => {
                    drawEverything();
                    animatePoint();
                    updateHint("📐 Visualisation triangle de pente active");
                }
            },
            {
                title: "Graphique de f'(x)",
                explanation: "La courbe verte montre f'(x) - la fonction dérivée traçant toutes les pentes.",
                audio: "La courbe verte montre f prime de x, traçant la pente en chaque point.",
                action: () => {
                    showDerivative = true;
                    buildDerivativeGraph();
                    updateHint("📈 Fonction dérivée f'(x) construite");
                }
            },
            {
                title: "Points Critiques",
                explanation: "Où f'(x) = 0, la fonction originale f(x) a des points minimum ou maximum.",
                audio: "Où f prime égale zéro, la fonction originale a des points critiques.",
                action: () => {
                    highlightCriticalPoints();
                    updateHint("🎯 Points critiques mis en évidence - f'(x) = 0");
                }
            },
            {
                title: "Mode Interactif",
                explanation: "Cliquez et glissez pour explorer ! L'interaction souris montre le calcul de dérivée en temps réel.",
                audio: "Maintenant explorez librement ! Cliquez et glissez pour voir comment les dérivées changent en temps réel.",
                action: () => {
                    enableInteraction();
                    updateHint("🖱️ Mode interactif : Cliquez et glissez sur la courbe !");
                }
            }
        ];

        // Fonctions de dessin
        function drawAxes() {
            ctx.clearRect(0, 0, 500, 500);
            ctx.strokeStyle = 'rgba(255, 255, 255, 0.3)';
            ctx.lineWidth = 1;

            // Axes
            ctx.beginPath();
            ctx.moveTo(50, centerY);
            ctx.lineTo(450, centerY);
            ctx.moveTo(centerX, 50);
            ctx.lineTo(centerX, 450);
            ctx.stroke();

            // Grid
            ctx.strokeStyle = 'rgba(255, 255, 255, 0.1)';
            for (let i = -2; i <= 2; i++) {
                if (i !== 0) {
                    const x = centerX + i * 100;
                    const y = centerY + i * 100;
                    ctx.beginPath();
                    ctx.moveTo(x, 50);
                    ctx.lineTo(x, 450);
                    ctx.moveTo(50, y);
                    ctx.lineTo(450, y);
                    ctx.stroke();
                }
            }

            // Labels
            ctx.fillStyle = 'rgba(255, 255, 255, 0.6)';
            ctx.font = '12px Arial';
            ctx.textAlign = 'center';

            for (let i = -2; i <= 2; i++) {
                if (i !== 0) {
                    const x = centerX + i * 100;
                    ctx.fillText((i * 50).toString(), x, centerY + 20);
                    
                    const y = centerY - i * 50;
                    ctx.fillText((i * 25).toString(), centerX - 20, y + 5);
                }
            }

            ctx.fillStyle = '#00f5ff';
            ctx.font = '16px Arial';
            ctx.fillText('x', 460, centerY + 20);
            ctx.fillText('f(x)', centerX - 30, 40);
        }

        function drawFunction() {
            ctx.strokeStyle = '#00f5ff';
            ctx.lineWidth = 3;
            ctx.beginPath();
            
            let firstPoint = true;
            for (let x = -100; x <= 100; x += 1) {
                const y = f(x);
                const canvasX = toCanvasX(x);
                const canvasY = toCanvasY(y);
                
                if (canvasY >= 50 && canvasY <= 450) {
                    if (firstPoint) {
                        ctx.moveTo(canvasX, canvasY);
                        firstPoint = false;
                    } else {
                        ctx.lineTo(canvasX, canvasY);
                    }
                }
            }
            ctx.stroke();
        }

        function drawPoint() {
            const y = f(currentX);
            const canvasX = toCanvasX(currentX);
            const canvasY = toCanvasY(y);

            // Point principal
            ctx.fillStyle = '#ff4444';
            ctx.beginPath();
            ctx.arc(canvasX, canvasY, 8, 0, 2 * Math.PI);
            ctx.fill();

            // Effet glow
            ctx.strokeStyle = '#ff4444';
            ctx.lineWidth = 2;
            ctx.beginPath();
            ctx.arc(canvasX, canvasY, 12, 0, 2 * Math.PI);
            ctx.stroke();

            updateValues();
        }

        function drawTangent() {
            if (!showTangent) return;

            const y = f(currentX);
            const slope = fPrime(currentX);
            const canvasX = toCanvasX(currentX);
            const canvasY = toCanvasY(y);

            const dx = 60;
            const dy = slope * dx / 2;
            
            const x1 = canvasX - dx;
            const y1 = canvasY + dy;
            const x2 = canvasX + dx;
            const y2 = canvasY - dy;

            ctx.strokeStyle = '#ffff00';
            ctx.lineWidth = 2;
            ctx.setLineDash([5, 5]);
            ctx.beginPath();
            ctx.moveTo(x1, y1);
            ctx.lineTo(x2, y2);
            ctx.stroke();
            ctx.setLineDash([]);

            if (currentStep >= 3) {
                drawSlopeTriangle(canvasX, canvasY, slope);
            }
        }

        function drawSlopeTriangle(pointX, pointY, slope) {
            const dx = 40;
            const dy = slope * dx / 2;

            ctx.strokeStyle = '#ffff00';
            ctx.lineWidth = 1;
            ctx.fillStyle = 'rgba(255, 255, 0, 0.2)';

            ctx.beginPath();
            ctx.moveTo(pointX, pointY);
            ctx.lineTo(pointX + dx, pointY);
            ctx.lineTo(pointX + dx, pointY - dy);
            ctx.closePath();
            ctx.fill();
            ctx.stroke();

            ctx.fillStyle = '#ffff00';
            ctx.font = '12px Arial';
            ctx.textAlign = 'center';
            ctx.fillText('Δx', pointX + dx/2, pointY + 15);
            ctx.fillText('Δy', pointX + dx + 15, pointY - dy/2);
        }

        function drawDerivativeGraph() {
            if (!showDerivative) return;

            ctx.strokeStyle = '#44ff44';
            ctx.lineWidth = 2;
            ctx.beginPath();
            
            let firstPoint = true;
            for (let x = -100; x <= 100; x += 1) {
                const y = fPrime(x);
                const canvasX = toCanvasX(x);
                const canvasY = toCanvasY(y);
                
                if (canvasY >= 50 && canvasY <= 450) {
                    if (firstPoint) {
                        ctx.moveTo(canvasX, canvasY);
                        firstPoint = false;
                    } else {
                        ctx.lineTo(canvasX, canvasY);
                    }
                }
            }
            ctx.stroke();

            // Point correspondant
            const derivativeY = fPrime(currentX);
            const derivativeCanvasX = toCanvasX(currentX);
            const derivativeCanvasY = toCanvasY(derivativeY);

            ctx.fillStyle = '#44ff44';
            ctx.beginPath();
            ctx.arc(derivativeCanvasX, derivativeCanvasY, 6, 0, 2 * Math.PI);
            ctx.fill();
        }

        function highlightCriticalPoints() {
            const criticalX = 0;
            const criticalY = f(criticalX);
            const canvasX = toCanvasX(criticalX);
            const canvasY = toCanvasY(criticalY);

            ctx.strokeStyle = '#ff00f5';
            ctx.lineWidth = 3;
            ctx.beginPath();
            ctx.arc(canvasX, canvasY, 15, 0, 2 * Math.PI);
            ctx.stroke();

            ctx.fillStyle = '#ff00f5';
            ctx.font = '14px Arial';
            ctx.textAlign = 'center';
            ctx.fillText('f\'(x) = 0', canvasX, canvasY - 25);
            ctx.fillText('Minimum', canvasX, canvasY - 40);
        }

        function drawEverything() {
            drawAxes();
            drawFunction();
            if (showDerivative) {
                drawDerivativeGraph();
            }
            drawTangent();
            drawPoint();
            
            if (currentStep >= 5) {
                highlightCriticalPoints();
            }
        }

        function updateValues() {
            const y = f(currentX);
            const slope = fPrime(currentX);

            document.getElementById('currentX').textContent = currentX.toFixed(1);
            document.getElementById('currentY').textContent = y.toFixed(1);
            document.getElementById('currentSlope').textContent = slope.toFixed(2);
            document.getElementById('currentDerivative').textContent = slope.toFixed(2);
        }

        function updateHint(text) {
            document.getElementById('hintBox').innerHTML = text;
        }

        function animatePoint() {
            let animationStep = 0;
            const animate = () => {
                currentX = -80 + animationStep * 3;
                if (currentX > 80) currentX = -80;
                
                drawEverything();
                animationStep = (animationStep + 1) % 54;
                
                if (currentStep === 3 && !autoMode) {
                    setTimeout(animate, 120);
                }
            };
            animate();
        }

        function buildDerivativeGraph() {
            let buildStep = 0;
            const buildAnimation = () => {
                if (buildStep <= 100) {
                    drawEverything();
                    buildStep += 2;
                    setTimeout(buildAnimation, 30);
                }
            };
            buildAnimation();
        }

        function enableInteraction() {
            canvas.style.cursor = 'pointer';
            canvas.addEventListener('mousedown', startDrag);
            canvas.addEventListener('mousemove', drag);
            canvas.addEventListener('mouseup', stopDrag);
            canvas.addEventListener('mouseleave', stopDrag);
        }

        function startDrag(event) {
            const rect = canvas.getBoundingClientRect();
            const mouseX = event.clientX - rect.left;
            const x = fromCanvasX(mouseX);
            
            if (x >= -100 && x <= 100) {
                isDragging = true;
                currentX = x;
                drawEverything();
                playTone(330, 100);
            }
        }

        function drag(event) {
            if (!isDragging) return;
            
            const rect = canvas.getBoundingClientRect();
            const mouseX = event.clientX - rect.left;
            const x = fromCanvasX(mouseX);
            
            if (x >= -100 && x <= 100) {
                currentX = x;
                drawEverything();
            }
        }

        function stopDrag() {
            isDragging = false;
        }

        // Fonctions principales
        function nextStep() {
            if (currentStep < steps.length - 1) {
                currentStep++;
                playTone(440 + currentStep * 50, 200);
                executeStep();
            } else {
                speak("Démonstration terminée ! Consultez le dépôt GitHub pour plus de visualisations mathématiques interactives.", 1);
                updateHint("🎉 Démo terminée ! Explorez le code source sur GitHub");
                playTone(880, 400);
            }
        }

        function executeStep() {
            const step = steps[currentStep];
            
            document.getElementById('nextBtn').textContent = 
                currentStep === steps.length - 1 ? "🎉 Terminer" : "▶️ Suivant";
            
            step.action();
            
            setTimeout(() => {
                if (!autoMode || currentStep === 0) {
                    speak(step.audio, 1);
                }
            }, 500);
        }

        function toggleAutoMode() {
            autoMode = !autoMode;
            const autoBtn = document.getElementById('autoBtn');
            
            if (autoMode) {
                autoBtn.textContent = "⏸️ Pause";
                autoBtn.style.background = 'linear-gradient(45deg, #ff4444, #cc0000)';
                startAutoMode();
                playTone(550, 200);
            } else {
                autoBtn.textContent = "🤖 Auto";
                autoBtn.style.background = 'linear-gradient(45deg, #00f5ff, #0088cc)';
                clearInterval(autoInterval);
                playTone(440, 200);
            }
        }

        function startAutoMode() {
            autoInterval = setInterval(() => {
                if (currentStep < steps.length - 1) {
                    nextStep();
                } else {
                    toggleAutoMode();
                    speak("Mode automatique terminé. Explorez maintenant de manière interactive !", 1);
                }
            }, 3500);
        }

        function toggleSound() {
            soundEnabled = !soundEnabled;
            const soundBtn = document.getElementById('soundBtn');
            
            if (soundEnabled) {
                soundBtn.textContent = "🔊 Son";
                soundBtn.style.background = 'linear-gradient(45deg, #00f5ff, #0088cc)';
                speak("Son activé", 1);
            } else {
                soundBtn.textContent = "🔇 Muet";
                soundBtn.style.background = 'linear-gradient(45deg, #666, #444)';
            }
        }

        function resetDemo() {
            currentStep = 0;
            currentX = 0;
            showTangent = false;
            showDerivative = false;
            isDragging = false;
            
            if (autoMode) {
                toggleAutoMode();
            }
            
            document.getElementById('nextBtn').textContent = '▶️ Commencer';
            
            drawAxes();
            updateValues();
            updateHint("🖱️ Cliquez sur 'Commencer' pour débuter l'exploration !");
            
            playTone(330, 200);
            speak("Démo remise à zéro. Prêt à explorer les dérivées !", 1);
        }

        // Initialisation
        function init() {
            drawAxes();
            updateValues();
            
            setTimeout(() => {
                speak("Démo interactive des dérivées prête ! Cliquez sur Commencer quand vous êtes prêt à explorer.", 1);
            }, 1000);
        }

        // Démarrage
        window.addEventListener('load', () => {
            setTimeout(() => {
                audioContext.resume().then(() => {
                    init();
                });
            }, 500);
        });
    </script>
</body>
</html>
